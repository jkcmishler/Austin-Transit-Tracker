# Generate new VAPID keys with:
#   node -e "import('web-push').then(w => { const k = w.default.generateVAPIDKeys(); console.log('PUBLIC=' + k.publicKey); console.log('PRIVATE=' + k.privateKey); })"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT=mailto:you@example.com
